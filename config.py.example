import os

BOT_TOKEN = "ВАШ_ТОКЕН_БОТА"

ADMIN_IDS = [
    12345678,
]

TELEGRAM_API_URL = "https://api.telegram.org"

# XFI CONNECT updater is repository-bound; origin must point to this repository.
GITHUB_REPO_URL = "https://github.com/deilja/XFI_CONNECT.git"

# XFI AI Gateway. Provider API keys are stored and managed by XFI AI,
# not inside XFI CONNECT.
XFI_AI_BASE_URL = os.getenv("XFI_AI_BASE_URL", "http://127.0.0.1:8091")
XFI_AI_API_KEY = os.getenv("XFI_AI_API_KEY", "")
XFI_AI_MODEL = os.getenv("XFI_AI_MODEL", "")
XFI_AI_TIMEOUT = float(os.getenv("XFI_AI_TIMEOUT", "45"))
# Server-side bootstrap credential. Never commit or expose it to Telegram/Web UI.
XFI_CONNECT_BOOTSTRAP_TOKEN = os.getenv("XFI_CONNECT_BOOTSTRAP_TOKEN", "")
XFI_AI_KEY_FILE = os.getenv("XFI_AI_KEY_FILE", "data/xfi_ai_api_key")

DEFAULT_LIMIT_IP = 1
DEFAULT_TOTAL_GB = 1024 * 1024 * 1024 * 1024

RATE_LIMITS = {
    "commands_per_minute": 30,
    "critical_operations_per_minute": 5,
}

RETRY_CONFIG = {
    "max_attempts": 3,
    "delays": [1, 3, 9],
    "timeout_seconds": 15,
}

SQLITE_JOURNAL_MODE = "WAL"
SQLITE_SYNCHRONOUS = "NORMAL"
SQLITE_BUSY_TIMEOUT_MS = 10000
SQLITE_CACHE_SIZE_KB = 32768
SQLITE_TEMP_STORE = "MEMORY"
SQLITE_MMAP_SIZE_BYTES = 134217728
